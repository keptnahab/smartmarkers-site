# smartmarkers.tools

Static landing page for SmartMarkers — deployed via GitHub Pages.
Source of truth lives in the private SmartMarkers repo (`deploy/webpage/`);
this repo is a deploy artifact. The beta email gate runs elsewhere (PHP).
